import React from 'react';

const set_acc=()=>{
    return (
    <a>
        <h2>Settings + account management where user can update allergies-Jeff (to be done)!</h2>
    </a>
    
    );
}
export default set_acc;